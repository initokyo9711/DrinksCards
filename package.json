{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "vite-build": "vite build",
        "vercel-build": "npm run vite-build && composer install --no-dev && php artisan config:clear && php artisan cache:clear",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.2.2",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.1.0",
        "vite": "^6.0.0"
    },
    "dependencies": {
        "firebase": "^12.11.0",
        "tailwindcss": "^4.2.2"
    }
}
